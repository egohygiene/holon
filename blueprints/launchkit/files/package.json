{
  "name": "{{parameter.package_name}}",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.24.0",
  "engines": {
    "node": ">=24.15.0",
    "pnpm": ">=11.24.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --ssr src/static-entry.tsx --outDir .static-render && node scripts/render-static.mjs && node scripts/write-static-fallback.mjs && node scripts/check-performance-budget.mjs",
    "preview": "vite preview",
    "format:check": "biome check .",
    "lint": "oxlint --type-aware --deny-warnings --react-plugin --jsx-a11y-plugin --vitest-plugin src scripts vite.config.ts && node scripts/check-dependency-cycles.mjs",
    "typecheck": "tsc --build --pretty false",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm build"
  },
  "dependencies": {
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.9",
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.5",
    "@vitejs/plugin-react": "6.1.1",
    "dependency-cruiser": "18.2.0",
    "oxlint": "1.79.0",
    "oxlint-tsgolint": "7.0.2001",
    "typescript": "7.0.2",
    "vite": "8.2.2",
    "vitest": "4.1.11"
  }
}
